# AMO（addons.mozilla.org）签名凭据。
#
# 复制成 .env 再填，`.env` 已在 .gitignore 里。
#
# 🔴 这两个值等于「能以你的身份向 AMO 提交任何东西」。
#    别提交、别贴进聊天、别写进命令行参数（argv 对任何能列进程的东西都可见）。
#    `scripts/sign-firefox.mjs` 经环境变量传给子进程，且从不回显它们的值。
#
# 到这里生成：https://addons.mozilla.org/developers/addon/api/key/
#   - AMO_API_KEY    页面上叫 JWT issuer，形如 user:12345678:90
#   - AMO_API_SECRET 页面上叫 JWT secret，一长串十六进制
#
# ⚠️ secret 只在生成时显示一次，关掉页面就看不到了。当场存好。
#
# 用法：
#   set -a; . ./.env; set +a
#   npm run sign:firefox

AMO_API_KEY=
AMO_API_SECRET=
